services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: mizkyosia_fr
    env_file: .env
    ports:
      - "${PORT}:${PORT}"
    develop:
      watch:
        - path: .
          target: /app
          action: sync
          initial_sync: true